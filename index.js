import React from 'react';
import ReactDOM from 'react-dom/client';
import Grid from './Grid'; // Assuming Grid is the main component for your Mind Matrix game

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Grid />
  </React.StrictMode>
);
